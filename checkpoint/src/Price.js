import React from 'react';
import product from './product';

const ProductPrice= () => {
    return (
        <div>
            <h1>{product.price}</h1>
        </div>
    );
};

export default ProductPrice;