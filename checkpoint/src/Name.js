import React from 'react';
import  product  from './product';

const ProductName = () => {
    return (
        <div>
            <h1>{product.name}</h1>
        </div>
    );
};

export default ProductName;